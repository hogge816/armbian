# Rockchip RK3528 Hinlink HT2 / fastyumjin
source "${SRC}/config/boards/rockchip-rk3528-ht2.csc"

# Required for the matrix generator / board discovery
declare -g BOARD_NAME="RK3528 HT2 (fastyumjin)"
declare -g BOARD_VENDOR="rockchip"
declare -g BOARDFAMILY="rockchip64"
declare -g BOARD_MAINTAINER="hogge816"
declare -g INTRODUCED="2026"
declare -g BOOT_SOC="rk3528"
declare -g BOOT_FDT_FILE="rockchip/rk3528-hinlink-h28k.dtb"
declare -g KERNEL_TARGET="current,edge"
declare -g KERNEL_TEST_TARGET="current"
